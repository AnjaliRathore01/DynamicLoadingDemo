import React from "react";
import ReactDOM from "react-dom/client";
import ImageGroup from "./components/ImageGallary";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ImageGroup />
  </React.StrictMode>
);
